import { useColorModeValue } from "@chakra-ui/react";

// export const btnColorMode = useColorModeValue("primary.200", "accent.50");
// export const btnBgMode = useColorModeValue("primary.900", "accent.500");

function useStyles() {

   // no reason to create this hook


}